const db = require("../SQLiteDB/db");

const getPosts = (req, res) => {
  // req.PARAMETR - GLOBAL_POSTS or other...
};

const createPost = (req, res) => {};

module.exports = {createPost, getPosts};
